import Link from "next/link";
import { notFound } from "next/navigation";
import { supabaseAdmin } from "@/lib/supabaseAdmin";

type Props = {
  params: Promise<{
    id: string;
  }>;
};

export default async function ModulePage({
  params,
}: Props) {
  const { id } = await params;

  const { data: module } = await supabaseAdmin
    .from("learning_modules")
    .select("*")
    .eq("id", id)
    .single();

  if (!module) {
    notFound();
  }

  return (
    <main className="mx-auto max-w-5xl px-6 py-12">

      <Link
        href="/dashboard/learning"
        className="text-sm font-medium text-[#071A52] transition hover:underline"
      >
        ← Back to Learning Center
      </Link>

      <div className="mt-8 rounded-[36px] bg-white p-10 shadow-sm">

        <p className="text-sm font-semibold uppercase tracking-[0.3em] text-neutral-500">

          Module {module.order_number}

        </p>

        <h1 className="mt-4 text-5xl font-semibold text-black">

          {module.title}

        </h1>

        <p className="mt-6 text-xl leading-9 text-neutral-600">

          {module.description}

        </p>

      </div>

      <article className="mt-10 rounded-[36px] bg-white p-10 shadow-sm">

  <h2 className="text-4xl font-semibold">
    Why Take Profit Exists
  </h2>

  <p className="mt-8 text-xl leading-9 text-neutral-700">

    Before learning about Take Profit, it's important to understand
    how trading works.

    Whether you're interested in stocks, cryptocurrencies,
    foreign exchange, commodities like gold and crude oil,
    stock indices, or other financial markets, they all
    operate on the same fundamental principles.

  </p>

  <p className="mt-8 text-xl leading-9 text-neutral-700">

    Trading is simply the act of buying or selling a financial
    asset with the expectation that its price will change.

    If your prediction is correct, you make a profit.

    If your prediction is wrong, you make a loss.

  </p>

  <div className="mt-12 rounded-[32px] border border-neutral-200 bg-neutral-50 p-10">

    <h3 className="text-3xl font-semibold">

      Financial assets commonly traded

    </h3>

    <div className="mt-8 grid gap-4 md:grid-cols-2">

      <div className="rounded-2xl bg-white p-6">
        Stocks
      </div>

      <div className="rounded-2xl bg-white p-6">
        Foreign Exchange
      </div>

      <div className="rounded-2xl bg-white p-6">
        Cryptocurrencies
      </div>

      <div className="rounded-2xl bg-white p-6">
        Commodities
      </div>

      <div className="rounded-2xl bg-white p-6">
        Stock Indices
      </div>

      <div className="rounded-2xl bg-white p-6">
        Exchange Traded Funds
      </div>

      <div className="rounded-2xl bg-white p-6">
        Bonds
      </div>

      <div className="rounded-2xl bg-white p-6">
        Other Financial Instruments
      </div>

    </div>

  </div>

  <div className="mt-14 rounded-[32px] border border-[#071A52] bg-[#071A52] p-10 text-white">

    <h3 className="text-3xl font-semibold">

      The basic idea is simple

    </h3>

    <div className="mt-10 grid gap-8 md:grid-cols-2">

      <div className="rounded-3xl bg-white/10 p-8">

        <h4 className="text-2xl font-semibold">

          Buy

        </h4>

        <p className="mt-5 leading-8">

          You buy when you believe the price of an asset
          will increase.

        </p>

      </div>

      <div className="rounded-3xl bg-white/10 p-8">

        <h4 className="text-2xl font-semibold">

          Sell

        </h4>

        <p className="mt-5 leading-8">

          You sell when you believe the price of an asset
          will decrease.

        </p>

      </div>

    </div>

  </div>

  <div className="mt-14 rounded-[32px] border-l-4 border-[#071A52] bg-blue-50 p-8">

    <p className="text-lg leading-8 text-neutral-800">

      Regardless of what you trade, every transaction is based on
      the same objective.

      Buy low and sell high.

      Or sell high and buy back lower.

      The difference between those prices determines whether
      you make a profit or a loss.

    </p>

  </div>

</article>

      <section className="mt-10 rounded-[36px] bg-white p-10 shadow-sm">

        <h2 className="text-3xl font-semibold">

          Feedback

        </h2>

        <p className="mt-3 text-neutral-600">

          Complete feedback will be enabled when this module
          becomes available.

        </p>

        <div className="mt-10 space-y-8">

          <div>

            <p className="font-medium">

              How clear was this module?

            </p>

            <div className="mt-4 flex gap-3">

              {[1, 2, 3, 4, 5].map((star) => (

                <button
                  key={star}
                  className="flex h-12 w-12 items-center justify-center rounded-full border border-neutral-300 text-lg transition hover:border-[#071A52]"
                >
                  ★
                </button>

              ))}

            </div>

          </div>

          <div>

            <p className="font-medium">

              How useful was this module?

            </p>

            <div className="mt-4 flex gap-3">

              {[1, 2, 3, 4, 5].map((star) => (

                <button
                  key={star}
                  className="flex h-12 w-12 items-center justify-center rounded-full border border-neutral-300 text-lg transition hover:border-[#071A52]"
                >
                  ★
                </button>

              ))}

            </div>

          </div>

          <div>

            <label className="font-medium">

              Feedback

            </label>

            <textarea
              rows={6}
              placeholder="Share your feedback..."
              className="mt-4 w-full rounded-3xl border border-neutral-300 p-5 outline-none transition focus:border-[#071A52]"
            />

          </div>

          <button
            disabled
            className="rounded-full bg-neutral-300 px-8 py-4 font-semibold text-white"
          >
            Submit Feedback
          </button>

        </div>

      </section>

    </main>
  );
}
